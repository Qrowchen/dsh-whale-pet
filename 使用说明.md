# 大肥鱼桌宠 · 可迁移版使用说明

这个包是**免安装**的：解压后双击即可运行，目标电脑**不需要装 Node.js、不需要联网安装依赖**。

## 一、快速开始

1. 把 `大肥鱼桌宠` 整个文件夹解压到任意位置（桌面、D 盘都行，**路径里不要有特殊符号**）
2. 进入文件夹，先配置 API Key（见下）
3. 双击 **`启动-静默.vbs`**（推荐，无黑窗）或 `启动.bat`（会显示日志，排错用）

挂件会出现在屏幕右下角。

## 二、配置 API Key（必须）

打开 `resources\app\config.json`，填入你自己的 DeepSeek API Key：

```json
{
  "apiKey": "sk-在这里填你的Key",
  "scale": 1.5,
  "exprVer": "v2",
  "model": "deepseek-flash",
  "soundOn": true,
  "vol": 0.9
}
```

| 字段 | 说明 |
|---|---|
| `apiKey` | DeepSeek API Key（在 https://platform.deepseek.com 申请）。**只保存在本机这个文件里**，不会上传。 |
| `scale` | 挂件大小，0.6 ~ 2.5 |
| `exprVer` | 表情素材版本：`v1` / `v2` / `v3` / `v4` |
| `model` | 对话模型，**只能是 `deepseek-flash` 或 `deepseek-v4-pro`** |
| `soundOn` / `vol` | 音效开关与音量 |

> 也可以用环境变量 `DEEPSEEK_API_KEY`，它的优先级高于配置文件。

**不填 Key 也能运行**，只是余额显示「获取失败」、对话不可用。

## 三、操作方式

| 操作 | 效果 |
|---|---|
| 点挂件身体 | 按压反馈（换表情 + 音效） |
| 拖动挂件身体 | 把挂件移到屏幕任意位置 |
| 鼠标移到挂件上 | 显示菜单按钮（问答 / 表情 / 尺寸 / 音效） |
| 点气泡 | 余额 → 峰谷 → 台词 → 关闭（循环） |
| 退出 | 右键任务栏图标，或 `Alt+F4` |

窗口本身是整屏大小的透明窗口，只有挂件和展开的菜单/面板接收鼠标，
**其余区域全部穿透到桌面**，所以不会挡住其他软件的点击。

## 四、再次双击启动器 = 重启

再点一次 `启动-静默.vbs`，它会先结束正在运行的旧实例（连同所有子进程）再启动新的。
**万一卡住了，不用去任务管理器，再点一次就行。**

## 五、系统要求

- Windows 10 / 11 64 位
- 无需安装任何运行库（Electron 运行时已内置于包中）
- 首次运行 Windows 可能弹出防火墙提示，允许即可（应用不监听端口，只是 Electron 的常规行为）

## 六、目录说明

```
大肥鱼桌宠/
├── WhalePet.exe            主程序（Electron 运行时）
├── 启动-静默.vbs           推荐：无黑窗启动 / 重启
├── 启动.bat                排错用：显示日志、出错不闪退
├── resources/
│   └── app/                应用本体
│       ├── main.js         主进程
│       ├── preload.js      IPC 桥
│       ├── config.json     你的配置（含 API Key）
│       ├── renderer/       界面代码
│       └── assets/         形象图 + 四套表情 + 音效
└── *.dll / *.pak / locales  Electron 运行时文件（不要删）
```

## 七、常见问题

**挂件不出现 / 闪退**
用 `启动.bat` 启动，它会停下来显示错误内容。日志也写在同目录 `_launch.log`。

**余额显示「获取失败」**
1. 检查 `config.json` 里的 `apiKey` 是否正确
2. 检查网络能否访问 `api.deepseek.com`
3. 如果短时间内频繁查询，DeepSeek 可能限流，等一会儿再试

**没有声音**
1. 检查 `config.json` 里 `soundOn` 是否为 `true`、`vol` 是否大于 0
2. 菜单里可以切换「音效」类型（小黄鸭 / 音效1）

**点挂件没反应**
确认没有其他窗口盖在上面；挂件在屏幕上任何位置都应可点。

**想换形象 / 表情**
替换 `resources\app\assets\` 下的图片：
- `DSniang1.png` —— 主形象
- `v1` ~ `v4` 子目录 —— 各套表情（**800×800 透明背景 PNG**）
文件名不能改，否则挂件找不到对应表情。

**想改回默认配置**
删除 `resources\app\config.json`，把 `config.example.json` 复制一份改名即可。

## 八、数据与隐私

- API Key 只存在本机 `config.json`（或环境变量），**不经过任何第三方服务器**
- 对话记录存在 `resources\app\chat-data\`（可随时删除）
- 今日消耗记账存在 `resources\app\usage-ledger.json`
- 当前实例进程号存在 `resources\app\.whale-pid`（正常退出会自动删除）

## 九、卸载

直接删除整个文件夹即可，不写注册表、不装服务、不留残留
（唯一例外：Windows 防火墙可能留有允许规则，可在防火墙设置里删除）。
