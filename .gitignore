# ---------------------------------------------------------------------------
# 本地配置：含 DeepSeek API Key，绝对不能提交
# ---------------------------------------------------------------------------
config.json

# ---------------------------------------------------------------------------
# 运行期产生的数据（每台机器各不相同）
# ---------------------------------------------------------------------------
usage-ledger.json
.whale-pid
chat-data/
_launch.log
_shot.png

# 诊断/排查过程产生的临时文件
_diag-*.txt
_diag-*.mjs
_probe-*.mjs
_dt.mjs
_*.log
# 本地用的推送/上传重试脚本与发布说明草稿（针对本机这台机器的网络环境）
_*.ps1
_relnotes.md

# ---------------------------------------------------------------------------
# 依赖与打包产物
#   node_modules 里有 electron（约 380MB），clone 后 npm install 即可恢复
#   dist 是打包好的可分发产物（446MB + 225MB zip），不进仓库
# ---------------------------------------------------------------------------
node_modules/
dist/

# ---------------------------------------------------------------------------
# 编辑器 / 系统文件
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
