# 把大肥鱼表情包换成主播 Q 版形象 · 实施方案

目标：**保持原有表情/姿势/构图不变，只替换角色形象**，产出可被桌宠直接使用的素材。

---

## 一、先明确素材规格（这是硬约束）

现有素材（`assets/` 下）：

| 内容 | 规格 |
|---|---|
| 尺寸 | **800 × 800** |
| 格式 | PNG，**透明背景** |
| 数量 | v1 = 18、v2 = 32、v3 = 32、v4 = 33，共 115 张 |
| 主形象 | `DSniang1.png`（每个版本目录里各有一张） |
| 命名 | 语义化英文名（`happy` / `angry` / `close_eyes`…），**不能改** |

**三条必须同时满足**，否则挂件会显示异常或缺图：尺寸 800×800、透明背景、文件名一致。

---

## 二、三条技术路线（按推荐度排序）

### 路线 A：ComfyUI + ControlNet + IP-Adapter（先试这条）

**为什么推荐**：不用训练，当天能出结果，适合先验证效果。

```
原表情图 ──► ControlNet(Lineart/OpenPose) ──► 锁定姿势与构图
                                                    │
主播形象参考图 ──► IP-Adapter ──► 注入角色特征 ──────┤
                                                    ▼
                                            生成（保持姿势）
                                                    ▼
                                        抠图 ──► 800×800 ──► 替换
```

关键配置：

| 项 | 建议值 | 说明 |
|---|---|---|
| ControlNet 类型 | `lineart_anime` 或 `canny` | chibi 风格用线稿控制最稳 |
| ControlNet 权重 | 0.7 ~ 1.0 | 太高会把原角色的线条也带过来 |
| IP-Adapter 权重 | 0.6 ~ 0.85 | 太高会僵，太低不像 |
| 重绘幅度 | 1.0（纯 ControlNet 生成） | 靠 ControlNet 保结构，而不是靠原图 |
| 采样步数 | 25 ~ 30 | |

### 路线 B：训练主播 LoRA（一致性最好，工作量大）

115 张要跨图保持角色一致，**LoRA 比 IP-Adapter 稳定得多**。

1. 收集主播形象图 **20 ~ 30 张**（不同角度、不同表情、清晰、背景干净）
2. 用 kohya_ss 或 ComfyUI 的训练节点练 LoRA（约 30 分钟 ~ 2 小时）
3. 用 **LoRA + ControlNet** 批量生成
4. 建议先练一版试 5 张，不满意就调数据集重练

**决策建议**：先跑路线 A 出 5 张。如果角色特征在不同表情间飘忽，再上路线 B。

### 路线 C：纯 img2img（最快，但最不像）

```
原图 ──► img2img(denoise 0.4~0.6) + prompt 描述主播形象 ──► 输出
```

缺点明显：denoise 低则改不动形象，denoise 高则姿势/表情跑掉。**只适合快速看个感觉**，不建议用于最终素材。

---

## 三、透明背景怎么保

原素材是透明 PNG，生成的图默认**不是**。三种处理方式：

| 方式 | 说明 |
|---|---|
| `rembg`（Python） | `pip install rembg`，命令行批量抠图，效果稳定 |
| ComfyUI 的 RMBG / BiRefNet 节点 | 在工作流里直接接在生成之后 |
| **用原图 alpha 当遮罩** | 原图的透明区域已经是精确轮廓，直接取 alpha 通道做 mask，**最准** |

推荐第三种：原图的 alpha 就是「角色该出现的位置」，用它裁掉生成图的背景，
能保证新素材的轮廓边缘和原图一致。

---

## 四、批量化（115 张必须脚本化）

ComfyUI 提供 HTTP API（`POST /prompt`），可以喂工作流 JSON 循环执行：

```python
import json, requests, os
from pathlib import Path

API = "http://127.0.0.1:8188/prompt"
SRC = Path("assets/v2")
OUT = Path("out/v2"); OUT.mkdir(parents=True, exist_ok=True)
workflow = json.load(open("workflow_api.json"))   # ComfyUI 导出的 API 格式

for img in sorted(SRC.glob("*.png")):
    wf = json.loads(json.dumps(workflow))
    # 把原图塞进 LoadImage 节点、输出名改成同名
    wf["<LoadImage节点id>"]["inputs"]["image"] = img.name
    wf["<SaveImage节点id>"]["inputs"]["filename_prefix"] = img.stem
    r = requests.post(API, json={"prompt": wf})
    print(img.name, r.status_code)
```

**建议流程**：先只跑 5 张 → 人工看效果、调参数 → 再全量跑。

---

## 五、验收清单

生成后逐项核对，任何一项不满足挂件都会出问题：

- [ ] 尺寸正好 **800 × 800**
- [ ] 背景**完全透明**（不是白底）
- [ ] **文件名与原图一致**（`happy.png` 还是 `happy.png`）
- [ ] 角色特征跨图一致（发色、瞳色、服装、比例）
- [ ] 表情语义正确（`angry` 要看得出生气，`close_eyes` 要闭眼）
- [ ] 边缘没有白边/锯齿（抠图残留）
- [ ] 逐张目视抽检，重点看 `close_eyes` / `half_closed_eyes`（眨眼帧，偏差会很明显）

替换前**先备份原 `assets` 目录**。

---

## 六、两个容易踩的坑

**1. 眨眼帧必须成对且尺寸一致**

`close_eyes.png` 与 `half_closed_eyes.png` 是动画帧，挂件会快速切换。
如果两张图里角色的位置/大小有偏移，眨眼时会「跳一下」。这两张要格外对齐。

**2. 主形象 `DSniang1.png` 要单独处理**

它不只是表情之一，还是：
- 挂件的默认形象
- **命中检测的基准图**（`hitCanvas` 用它的轮廓判断点击落在哪）

所以它的轮廓必须干净、角色必须居中。如果新形象的轮廓和它差异过大，
可能需要同步调整 `widget.js` 里 610×610 的命中画布逻辑（一般不用，但值得知道）。

---

## 七、如果不用 AI

| 方式 | 质量 | 成本 |
|---|---|---|
| 请画师按表情清单重绘 | 最高、最统一 | 115 张，数百 ~ 数千元 |
| AI 生成 + 人工修图 | 高 | 需要美术基础 |
| 3D 建模 + 多角度渲染 | 最高，还能做动画 | 需要建模能力 |
| Live2D | 能动，但挂件不支持 | — |

**折中做法**：只重绘常用的 20 ~ 30 张（`DSniang1`、`happy`、`angry`、`close_eyes`、
`exhausted`、`shy` 等），其余保留原图。挂件按文件名取图，缺哪张补哪张即可。

---

## 八、替换步骤

1. **备份**：把 `assets` 整个复制一份到别处
2. 新图按原文件名放入对应版本目录
3. 启动桌宠，菜单里切换表情版本逐个验证
4. 若某张显示不出来 → 检查文件名、尺寸、格式
5. 若不满意，用备份还原

> 桌宠的 `assets` 在 `resources\app\assets\`（打包版）
> 或项目根目录的 `assets\`（开发版）。
